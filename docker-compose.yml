services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: astrostats:latest
    restart: unless-stopped
    # No host port mappings so this works in Coolify and local worker mode.
    env_file:
      - path: .env
        required: false
    environment:
      PYTHONUNBUFFERED: "1"
